server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: mittelstandgpt
  # Backend selection (Workstream D): `local` (default, fully offline Ollama+Qdrant)
  # or `azure` (Azure OpenAI + Azure AI Search). Telemetry verbosity is an
  # orthogonal dimension: optionally add `dev` (export prompt/response content) or
  # `prod` (content export off, GDPR). Example: SPRING_PROFILES_ACTIVE=azure,prod
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  servlet:
    multipart:
      # Generous limits for document uploads.
      max-file-size: 50MB
      max-request-size: 50MB

# Actuator: health/info for the Docker healthcheck. Metrics/prometheus and trace
# export are added in the observability milestone.
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never
  # The Micrometer tracing bridge + OTLP exporter are on the classpath, but tracing
  # and span export stay OFF here so application behaviour is unchanged. The
  # observability milestone enables this and points it at self-hosted Langfuse.
  tracing:
    enabled: false
